# 项目结构说明

本项目采用 FastAPI 分层结构：

- `api/routes`：页面路由和接口路由
- `core`：配置、安全等基础能力
- `db`：数据库连接和初始化
- `models`：数据库模型
- `schemas`：请求和响应数据结构
- `services`：业务逻辑
- `templates`：HTML 页面
- `static`：CSS、JavaScript 等静态资源
